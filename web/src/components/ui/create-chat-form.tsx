import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "./card";

const createChatSchema = z
export function CreateChatForm(){
    const {} = useForm()
return(
    <Card>
        <CardHeader>
            <CardTitle>Criar Chats</CardTitle>
            <CardDescription>Crie um novo chat para começar a fazer perguntas e receber respostas da IA</CardDescription>
        </CardHeader>
        <CardContent>
            <form className="flex flex-col gap-4">

            </form>
        </CardContent>
    </Card>
);
}